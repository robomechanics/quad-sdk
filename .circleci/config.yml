jobs:
  build-melodic:
    working_directory: /catkin_ws/src
    docker:
      - image: ros:melodic-ros-core-bionic
    steps:
      - checkout
      - run:
          command: |
            apt-get update
            apt-get install -y build-essential
            apt-get install -y ros-melodic-tf python-rosdep python-rosinstall python-rosinstall-generator python-wstool
      - run:
          command: |
            source /opt/ros/melodic/setup.bash
            catkin_init_workspace
            chmod +x setup.sh
            ./setup.sh
            cd ..
            catkin_make
workflows:
  version: 1
  build-and-test:
    jobs:
      - build-melodic:
          filters:
            branches:
              only:
                - master
                - develop
                - melodic-devel
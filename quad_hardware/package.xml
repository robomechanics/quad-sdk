<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>quad_hardware</name>
  <version>0.1.0</version>
  <description>
    ros2_control hardware components for quad-sdk robots. Provides
    hardware_interface::SystemInterface plugins so the controller_manager
    drives real robots the same way it drives Gazebo. Currently: UnitreeSystem
    (Go2 / Go2-W, via the Unitree SDK2 DDS contract).
  </description>
  <maintainer email="ologandavid@gmail.com">ologandavid</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>hardware_interface</depend>
  <depend>pluginlib</depend>
  <depend>rclcpp</depend>
  <depend>rclcpp_lifecycle</depend>
  <depend>eigen</depend>

  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
